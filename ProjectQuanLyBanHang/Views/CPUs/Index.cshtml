@model IEnumerable<ProjectQuanLyBanHang.Models.CPU>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenCpu)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NhaSX)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MoTa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenCpu)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NhaSX)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MoTa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CpuId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CpuId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CpuId })
        </td>
    </tr>
}

</table>
